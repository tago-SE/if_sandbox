import React from "react";

const Index = props => <div className="if container">
  <h2 className="if heading medium center" id={props.blok.heading}>
    {props.blok.heading}
  </h2>
  <div className="if panels">
   
  </div>
</div>;

export default Index;
